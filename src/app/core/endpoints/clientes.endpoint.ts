import { BASEURL } from '../../../environments/environment';

export class ClientesEndpoint {
  // public static getClient = BASEURL + 'cliente' + 1 + 'id';
  public static rest = `${BASEURL}cliente`;
}
