import { BASEURL } from '../../../environments/environment';

export class ProveedoresEndpoint {
  // public static getUser = BASEURL + 'cliente' + 1 + 'id';
  public static rest = `${BASEURL}proveedor`;
}
